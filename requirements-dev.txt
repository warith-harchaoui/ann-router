# Development / CI dependencies: the core, the test runner, and every backend
# that installs cleanly with pip on macOS/Apple Silicon + Linux.
# On Apple Silicon install annoy via conda-forge (`conda install -c conda-forge
# python-annoy`) instead of pip — the pip source build is miscompiled there.
-r requirements.txt

pytest>=8
ruff>=0.5

# ANN engines (optional extras, pinned here so the recall suite exercises them)
hnswlib>=0.7,<1
faiss-cpu>=1.7
annoy>=1.17,<2
turbovec>=0.5
qdrant-client>=1.7
pgvector>=0.2
psycopg[binary]>=3.1

# The other three "doors"
click>=8.1,<9
fastapi>=0.111,<1
uvicorn>=0.30,<1
httpx>=0.27
