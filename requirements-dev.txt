# Development / CI dependencies for ann-router.
#
# Derived from pyproject.toml's [dev] extra (pytest + every ANN backend that
# installs cleanly with pip on macOS/Apple Silicon + Linux, plus the other
# four "doors": cli/api/mcp). Kept only for contributors who prefer
# `pip install -r requirements-dev.txt` -- edit the [dev] extra in
# pyproject.toml, not this file, to change what installs.
#
# On Apple Silicon install annoy via conda-forge (`conda install -c conda-forge
# python-annoy`) instead of pip -- the pip source build is miscompiled there.
-e .[dev]
ruff==0.15.21
